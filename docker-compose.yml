services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: ecfr
      POSTGRES_PASSWORD: ecfr
      POSTGRES_DB: ecfr_analyzer
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
